package main 

import "fmt"

var steps int = 30 // string type variable declaration and initialization for number of steps

var msg  = "Learning Go is my plan for now!" // string type variable declaration and initialization for message

func main() {
	fmt.Println(msg, steps, "its great!") // Print message along with steps
}